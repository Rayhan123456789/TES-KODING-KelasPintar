const id = "sang gajah - sang gajah - serigala - serigala - harimau"
function animal(id) {
    for (let i = 0; i < array.length; i++) {
        const element = array[i];
        if (animal.id==id) {
            return animal.name;
        }
    }
}
console.log(id)
// console.log()
